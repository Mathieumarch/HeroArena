<UserControl x:Class="HeroArena.Views.SpellsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource DarkBgBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Sorts"
                   Style="{StaticResource TitleText}"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Filtrer par héros :"
                       Style="{StaticResource BodyText}"
                       VerticalAlignment="Center"
                       Margin="0,0,12,0"/>
            <ComboBox ItemsSource="{Binding HeroNames}"
                      SelectedItem="{Binding FilterHeroName}"
                      Background="{StaticResource CardBgBrush}"
                      Foreground="{StaticResource TextPrimaryBrush}"
                      BorderBrush="{StaticResource PurpleBrush}"
                      Width="160"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredSpells}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource HeroCard}" Width="240">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}"
                                           Style="{StaticResource BodyText}"
                                           FontWeight="Bold"
                                           FontSize="15"
                                           Margin="0,0,0,6"/>
                                <TextBlock Style="{StaticResource SubText}">
                                    <Run Text="Dégâts : "/>
                                    <Run Text="{Binding Damage}"
                                         Foreground="{StaticResource DangerBrush}"
                                         FontWeight="Bold"/>
                                </TextBlock>
                                <TextBlock Text="{Binding Description}"
                                           Style="{StaticResource SubText}"
                                           TextWrapping="Wrap"
                                           Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>