<Window x:Class="HeroArena.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:HeroArena.Views"
        Title="Hero Arena"
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource DarkBgBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PanelBgBrush}"
                Padding="20,12">
            <Grid>
                <TextBlock Text="HERO ARENA"
                           Style="{StaticResource TitleText}"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="WelcomeText"
                           Style="{StaticResource SubText}"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- TabControl -->
        <TabControl Grid.Row="1"
                    x:Name="MainTab"
                    Background="{StaticResource DarkBgBrush}"
                    BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                    <Setter Property="Background" Value="{StaticResource PanelBgBrush}"/>
                    <Setter Property="FontSize"   Value="14"/>
                    <Setter Property="Padding"    Value="20,10"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource GoldBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TabControl.Resources>

            <TabItem Header="Héros">
                <views:HeroesTab x:Name="HeroesTabControl"/>
            </TabItem>

            <TabItem Header="Sorts">
                <views:SpellsTab x:Name="SpellsTabControl"/>
            </TabItem>

            <TabItem Header="Combat">
                <views:CombatView x:Name="CombatTabControl"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>